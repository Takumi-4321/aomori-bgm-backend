version: "3.8"

services:
  web:
    build: .
    container_name: aomori_bgm_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    # 【実務仕様】環境変数の指定（開発環境であることを明示）
    environment:
      - ENV=development
      - DATABASE_URL=sqlite:///./aomori_bgm.db
